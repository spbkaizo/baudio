.DS_Store

# Build artefacts. Everything here is produced by `make` and published by
# CI; see the Firmware section of README.md. Released .hex files are
# attached to GitHub Releases rather than committed.
*.o
*.elf
*.hex
*.lst
